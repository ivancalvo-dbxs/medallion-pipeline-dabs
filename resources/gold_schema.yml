resources:
  schemas:
    gold_schema:
      catalog_name: ${var.catalog}
      name: "${bundle.name}_gold"
      grants:
        - principal: ${var.data_engineers}
          privileges:
            - ALL_PRIVILEGES
        - principal: ${var.data_consumers}
          privileges:
            - SELECT

      comment: |
        # Gold schema.

        This schema was created by **${workspace.current_user.displayName}** from DABs.

        * The following group has ALL_PRIVILEGES grants:
        * **${var.data_engineers}**

        * The following group has SELECT grants:
        * **${var.data_consumers}**

        
